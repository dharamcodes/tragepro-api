meta {
  name: Get user by username
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/auth/find/:userName
  body: none
  auth: inherit
}

params:path {
  userName: admin01
}

script:post-response {
  const body = res.body;
  if (res.status === 200 && body) {
    setAuthUserVars(body);
  }
}
