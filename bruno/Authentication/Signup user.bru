meta {
  name: Signup user
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/signup
  body: json
  auth: none
}

body:json {
  {
    "email": "admin.mail1@tragepro.com",
    "userName": "admin01",
    "password": "admin01",
    "role": "SUPER_USER",
    "isActive": true
  }
}

script:post-response {
  const body = res.body;
  if (res.status === 200 && body) {
    setAuthUserVars(body);
  }
}
