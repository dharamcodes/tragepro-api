server:
  port: 8082

spring:
  mongodb:
    uri: ${MONGODB_URI:mongodb://admin:admin@localhost:27017/tragepro}
    database: tragepro
logging:
  level:
    root: INFO
    org.springframework: INFO

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173,http://localhost:8082}
