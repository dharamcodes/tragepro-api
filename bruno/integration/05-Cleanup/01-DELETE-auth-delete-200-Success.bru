meta {
  name: 01-DELETE-auth-delete-200-Success
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/api/v1/auth/delete/{{testUserName}}
  body: none
  auth: inherit
}

script:post-response {
  expect(res.getStatus()).to.equal(202);
}
