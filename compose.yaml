services:
  mongodb:
    image: 'mongo:latest'
    environment:
      - 'MONGO_INITDB_DATABASE=tragepro'
      - 'MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_PASSWORD}'
      - 'MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_USERNAME}'
    ports:
      - '27017'
