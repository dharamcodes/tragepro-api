services:
  mongodb:
    image: 'mongo:latest'
    environment:
      - 'MONGO_INITDB_DATABASE=${MONGO_INIT_DATABASE:-tragepro}'
      - 'MONGO_INITDB_ROOT_PASSWORD=${MONGO_INIT_PASSWORD:-admin}'
      - 'MONGO_INITDB_ROOT_USERNAME=${MONGO_INIT_USERNAME:-admin}'
    ports:
      - '27017:27017'
