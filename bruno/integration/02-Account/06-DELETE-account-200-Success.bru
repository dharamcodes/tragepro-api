meta {
  name: 06-DELETE-account-200-Success
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/v1/account/{{testAccountId}}
  body: none
  auth: inherit
}

script:post-response {
  expect(res.getStatus()).to.equal(200);
}
