meta {
  name: 08-POST-strategy-run-400-Err-MissingFields
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/strategy/run
  body: json
  auth: inherit
}

body:json {
  {}
}

script:post-response {
  expect(res.getStatus()).to.equal(400);
}
