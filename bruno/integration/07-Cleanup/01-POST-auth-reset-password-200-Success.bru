meta {
  name: 01-POST-auth-reset-password-200-Success
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/reset-password/{{testUserName}}
  body: none
  auth: inherit
}

script:post-response {
  expect(res.getStatus()).to.equal(202);
}
